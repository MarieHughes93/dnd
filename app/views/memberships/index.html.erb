<h1> <%=@campaign.name %>'s Members </h1>
<%= render partial: '/memberships/form' %>

<% @memberships.each do |m|%>
<%= link_to "#{m.user.name}'s Profile", user_path(m.user)%>
<%# <%= link_to "#{m.user.character.name}'s Profile", user_character_path(m.user, m.user.character) %>
<%end%>
